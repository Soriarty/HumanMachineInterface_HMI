<UserControl x:Class="HM_Interface_Visu.Assets.FlyOutMenuButton"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes" 
             xmlns:local="clr-namespace:HM_Interface_Visu.Assets"
             mc:Ignorable="d" 
             d:DesignHeight="50" d:DesignWidth="170">
    
    <DockPanel>
        <Grid DockPanel.Dock="Top" VerticalAlignment="Top" Height="45">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="50"/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <materialDesign:PackIcon x:Name="ButtonIcon" Foreground="{DynamicResource AccentColorBrush}" Grid.Column="0" Width="24" Height="24" Kind="CheckboxBlankCircleOutline" HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" />
            <TextBlock x:Name="ButtonContent" Style="{StaticResource MaterialDesignHeadlineTextBlock}" Text="Button" Grid.Column="1" HorizontalAlignment="Center" VerticalAlignment="Center" />
        </Grid>
        <Rectangle Fill="{DynamicResource MaterialDesignCardBackground}" />
    </DockPanel>
</UserControl>
